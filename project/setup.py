#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='shared',
      version='0.0.1',
      description='Dataframe Builder and Analysis',
      author='Dale R. Merz Jr.',
      author_email='dmerz75@github',
      # packages=find_packages(exclude=["*.tests", "*.tests.*", "tests.*"]),
      license='LICENSE',
    )
